import SwiftUI

struct WorkRhythmRootView: View {
    var body: some View {
        Text("工作节奏")
            .font(.title3)
            .foregroundStyle(.tertiary)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .padding(ShellMetrics.contentMargin)
    }
}
